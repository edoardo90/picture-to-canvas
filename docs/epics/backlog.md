# Backlog Items

## 1. Warn if paper/image ratio is suboptimal
- Show a warning button if the user selects a paper orientation (e.g. horizontal) that is not optimal for the loaded image (e.g. vertical image).

## 2. Save project state
- Add a Save button that stores all meaningful data: list of points, style, image (if possible), paper size, and fit-mode.

## 3. Toggle point visibility
- Add a button to toggle visibility of all points: ON = all points visible, OFF = no points visible.
